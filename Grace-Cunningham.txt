Task one: https://codepen.io/gracecunningham/pen/jOMvRE://codepen.io/gracecunningham/pen/jOMvREP
Task two: https://codepen.io/gracecunningham/pen/QWKZrLe


1. What is Semantic HTML? It is the language of coding. 
    2. What is HTML used for? It is used for structuring your website. 
    3. What is an attribute and where do we put it? an attribute is at the end of a 
    4. What is the h1 tag used for? How many times should I use it on a page? H1 is the header. you should put it in the beginning and the end. 
    5. Name two tags that have required attributes. <div> and <section>
    6. What do we put in the head of our HTML document? <HTML>
    7. What is an id? An identifier. 
    8. What elements can I add an id to? 2 
    9. How many times can I use the same id on a page? not more than one more. 
    10. What is a class? A class is a group of elements that are the same. 
    11. What elements can I add a class to? : and ;
    12. How many times can I use the same class on a page? one 
    13. How do I get my link to open in a new tab? ctrl then click on the link 
    14. What is the alt attribute in the image tag used for? it is used to specify differnt texts. 
    15. How do I reference an id? 
    16. What is the difference between a section and a div? 
    17. What is CSS used for? to style your website. is a keyword added to the selector that specifies the select state of the element. 
    18. How to we select an element? Example - every h2 on the page <>
    19. What is the difference between a class and an id? - Give me an example of when I might use each one. 
    20. How do we select classes in CSS? with a period. 
    21. How do we select a p element with a single class of “human””?
    22. What is a parent child selector? When would this be useful? This is the section inside a <DIV>. 
    23. How do you select all links within a div with the class of sidebar? 
    24. What is a pseudo selector? is a keyword added to a selector that specifies a special state of the selected elements. 
    25. What do we use the change the spacing between lines? 
    26. What do we use to change the spacing between letters?
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? 
    28. How do I add a 1px border around my div that is dotted and black? 
    29. How do I select everything on the page? ctrl s
    30. How do I write a comment in CSS? <---->
    31. How do I find out what file I am in, when I am using the command line? 
    32. Using the command line - how do I see a list of files/folders in my current folder?
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
    34. Why should I use version control? 
    35. How often should I commit to github? everyday. 
    36. What is the command we would use to push our repo up to github? push
    37. Walk me through Lambda's git flow. Fork, CLone, Code, use terminal, pull, push, branch, commit. 